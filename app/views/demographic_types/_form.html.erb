<%= form_for(@demographic_type) do |f| %>
  <% if @demographic_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@demographic_type.errors.count, "error") %> prohibited this demographic_type from being saved:</h2>

      <ul>
      <% @demographic_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :acsepted_values %><br />
    <%= f.text_field :acsepted_values %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
